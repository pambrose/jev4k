[project]
name = "website"
version = "0.1.0"
description = "Documentation site for jev4k, built with Zensical"
requires-python = ">=3.14"
dependencies = []

[dependency-groups]
dev = [
    "zensical>=0.0.62",
]
